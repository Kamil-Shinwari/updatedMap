import 'package:flutter/material.dart';
class PlanARoute extends StatelessWidget {
  const PlanARoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(child: Column(children: [
        
      ])),
    );
  }
}